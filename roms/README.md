# 恢复的原始用户文件

这两份文件由仓库所有者先前提交。本次按用户要求从清理前提交 `6c0c35c` 恢复，集中放在这里；复用原 Git blob，不改写二进制，不从第三方下载新的游戏文件。

| 文件 | 字节数 | 原 Git blob SHA-1 | 用途 |
| --- | ---: | --- | --- |
| `red-star-2020-08-18.gb` | 1,048,576 | `3cc54b4c888b0cbda1c27996806ae4729199b377` | 当前 PyBoy Agent 支持的 Red Star |
| `1636 - Pokemon Fire Red (U)(Squirrels).gba` | 16,777,216 | `a0d042c360c18b860da3ef71ebac0d826e136cdc` | 恢复用户资产；当前控制器不支持 FireRed |

Git blob SHA-1 包括对象头，不等于文件内容 SHA-1。Red Star 内容 SHA-1 为 `e2d564a5172d38e44b4f4b8f7d9db92f644cf5e9`，运行适配器会检查该值。

默认入口优先读取已有的根目录 Red Star（兼容旧用法），否则使用本目录的恢复文件；`--rom` 永远可以显式覆盖选择。不移动用户 `.env`、存档或本地会话。

`.gitignore` 只对上面两份已经上传的文件例外，其他 ROM 仍默认忽略。恢复文件不代表项目授予游戏再分发许可；这里只记录用户原资产的身份与技术适配边界。
